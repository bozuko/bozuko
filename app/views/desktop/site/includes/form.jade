p Interested in seeing a demonstration? Fill in the form below and we will get back to you as soon as possible.
  |  You can also email us at 
  a(href="mailto:questions@bozuko.com") questions@bozuko.com
  |  if you prefer.

form.contact-form(action="/enterprise/form",method="post")
  .field-wrap
    label(for="name") Name
    .field
      input#name(name="name",value=locals.name,required="required")
  
  .field-wrap
    label(for="email") Email
    .field
      input#email(name="email",value=locals.email,type="email",required="required")
      
  .field-wrap
    label(for="message") Message
    .field
      textarea#message(name="message",required="required")!= locals.message
  
  .buttons
    input.big-button(type="submit", value="Send")
